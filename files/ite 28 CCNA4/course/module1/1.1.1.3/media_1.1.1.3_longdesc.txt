Figure 1 shows two routers labelled ‘Site A” and ‘Site B’ separated by a cloud, and connected to each other by a single line. Figure 2. displays 4 router connected to each other through a cloud, in a hub-and-spoke topology, with the Hub router labelled ‘hub’ and the spoke routers labelled as spoke A – C. Figure 3 displays 4 routers connected to each other, in the cloud, in a full mesh topology through a cloud, the routers are labelled as Sites A – D. Figure 4 displays 2 routers as the hub routers, connecting in a full mesh topology to 3 spoke routers (A-C) through the cloud. 